import 'package:flutter/material.dart';

class Consent extends StatelessWidget {
  build(context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          '[ชื่อโครงการ]',
        ),
      ),
      body: Container(
        child: Text('Consent'),
      ),
    );
  }
}
